{
    "mcpServers": {
        "filesystem": {
            "command": "docker",
            "args": [
                "run",
                "-i",
                "--rm",
                "--mount",
                "type=bind,src=C:/dev/mcp,dst=/projects/workspace",
                "mcp/filesystem",
                "/projects"
            ]
        }
    }
}
