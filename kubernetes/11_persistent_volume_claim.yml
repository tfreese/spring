apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: pv-claim
    #namespace: demo
spec:
    storageClassName: local-storage
    volumeMode: Filesystem
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
